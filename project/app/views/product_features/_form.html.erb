<%= form_with(model: product_feature) do |form| %>
  <% if product_feature.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_feature.errors.count, "error") %> prohibited this product_feature from being saved:</h2>

      <ul>
        <% product_feature.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
